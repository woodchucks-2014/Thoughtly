<nav>
<div id="title">thoughtly.io</div>
<div id="fa">
	<ul>
    <% if @current_user %>
    <li id="c"><%= link_to icon('sign-in fa-2x'), logout_path(@current_user) %></li>
    <% end %>
    <li id="a"><%= link_to icon('home fa-2x'), root_path %></li>
    <li>Session: <%= session[:user_id] %></li>
  </ul>
</div>
</nav>
